import React from "react";
import AHeader from "./AProduct/AHeader";

export default function ATotem() {
  return (
    <React.Fragment>
      <AHeader />
      <main style={{
        display: 'flex',
        flexDirection: 'column'
      }}>
        
      </main>
    </React.Fragment>
  )
}